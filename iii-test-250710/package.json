{
  "name": "gsap-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass:watch": "sass --watch src/style.scss:style.css --style=compressed",
    "sass:build": "sass src/style.scss:style.css --style=compressed",
    "start": "concurrently -n VITE,SASS -c cyan,magenta \"npm:dev\" \"npm:sass:watch\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "sass": "^1.89.2",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  }
}